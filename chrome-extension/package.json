{
  "name": "freeai-chrome",
  "version": "0.2.0",
  "private": true,
  "description": "FreeAI.fyi Chrome extension — earn while you use ChatGPT, Claude, and Gemini.",
  "scripts": {
    "lint": "node -c src/ads.js && node -c src/content.js && node -c src/background.js && node -c popup/popup.js && echo \"syntax ok\"",
    "test": "node test/run.js"
  }
}
