# Storage 📦

Shelved-but-working pieces. Nothing in here is wired into the main product right now.

| Path | What it is | Status |
| --- | --- | --- |
| [`chrome-extension/`](chrome-extension/) | The Chrome (MV3) extension that shows sponsored lines on web agents (claude.ai, ChatGPT, Gemini…). Fully self-contained — loads unpacked and runs offline. | On ice while we focus on the exact VS Code replica with the 90% split. |

To revive the Chrome extension: `chrome://extensions` → Developer mode →
**Load unpacked** → select `storage/chrome-extension/`. Docs in its
[README](chrome-extension/README.md).
